<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- App content is rendered entirely in Jetpack Compose (see ui/theme/Theme.kt),
         this is only the pre-Compose window theme used while the activity launches. -->
    <style name="Theme.Androidgolibrespot" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
    </style>
</resources>
